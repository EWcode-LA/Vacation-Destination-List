var destination = document.querySelector("#destination_details_forms");
//add event listener
detailsForm.addEventListener("submit" handleFormSubmit);

function handleFormSubmit(event){
    event.preventDefault();
    
}